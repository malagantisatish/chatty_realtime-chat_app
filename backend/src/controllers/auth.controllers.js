export const signup = (reqest,response)=>{
    response.send("signup route")
}

export const login = (reqest,response)=>{
    response.send("login route")
}

export const logout = (reqest,response)=>{
    response.send("logout route")
}